import { Routes } from '@angular/router';
import { NoteeaseMainComponent } from './noteease-main.component';

export const routes: Routes = [
  { path: '', component: NoteeaseMainComponent, pathMatch: 'full' }
];
